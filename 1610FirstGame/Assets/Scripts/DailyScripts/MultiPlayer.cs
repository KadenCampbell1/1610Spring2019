using UnityEngine;

namespace DailyScripts
{
    public class MultiPlayer : MonoBehaviour
    {
        public int numberCharacter;

        public int buttonConfigNumber;
    
        // Start is called before the first frame update
        void Start()
        {
            //see how many players are playing
            //assign buttons for characters
        
        }

        // Update is called once per frame
        void Update()
        {
            //see if someone drops out
            //take characters off
        }
    }
}
